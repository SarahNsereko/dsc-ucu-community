import React from 'react';
import ReactDOM from 'react-dom';
// import { AppProvider } from "./pages/Context"; // Import the context provider
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    
      <App />

  </React.StrictMode>
);
